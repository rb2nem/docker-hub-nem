# Candidate Official Docker Hub Images for NEM

These docker configurations are submitted for inclusion as official 
Docker Hub images.

The doal is to be able to start a NEM Infrastucture Server instance with
```
docker run nem:nis
```
